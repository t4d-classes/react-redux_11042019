Exercise 6

1. Create a new component named Tool Header, display the header and h1 element from car tool in Tool Header. Pass the text of h1 element in Tool Header using props. Utilize the Tool Header in Car Tool.

2. Create a new component named Car Table. Move the table from Car Tool to Car Table. Utilize Car Table in Car Tool.

3. Create a new component named Car View Row. Move the Car Table data row for the car to the new component. Utilize the Car View Row component to display the car table in Car Table.

Bonus: Add a new column to the table with a header of Actions. In the Actions column, please a Delete button on each data row. When the button is clicked remove the car from the table.

